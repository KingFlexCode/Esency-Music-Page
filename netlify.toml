[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[[scheduled]]
  name = "sync-printful-merch-nightly"
  cron = "0 3 * * *" # Runs every day at 3am UTC
  function = "sync-printful-merch"
